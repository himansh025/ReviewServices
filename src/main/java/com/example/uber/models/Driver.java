package com.example.uber.models;import jakarta.persistence.Column;import jakarta.persistence.Entity;import jakarta.persistence.OneToMany;import lombok.*;import java.util.ArrayList;import java.util.List;@Getter@Setter@NoArgsConstructor@AllArgsConstructor@Builder@Entitypublic class Driver extends BaseModel {    private  String name;    @Column(nullable = false,unique = true)    private  String licenceNumber;//    1:n relationship mapped by is used bcz the spring will confuse to identify the booking is for the user or the driver and hibernet create not identify and create new table driver_booking    @OneToMany(mappedBy = "booking") //here we have to define to whom this booking is pass or driver    private List<Booking> bookings= new ArrayList<>();}
package com.example.uber.models;import jakarta.persistence.*;import lombok.*;import java.util.Date;@Getter@Setter@NoArgsConstructor@AllArgsConstructor@Builder@Entitypublic  class Booking extends BaseModel {    // 1:1 mapping btw the booking and review    @OneToOne(cascade={CascadeType.PERSIST,CascadeType.REMOVE})            // when our booking table is depends on the review then instead of doing the review save first then use hte obj in the booking saved soring cascade done it manually//    remove if booking is remove review associated  is also removed    private Review review;    @Enumerated(value = EnumType.STRING)    private BookingStatus bookingStatus;    @Temporal(value = TemporalType.TIMESTAMP)    private Date startTime;    @Temporal(value = TemporalType.TIMESTAMP)    private Date endTime;    private Long totalDistance;    @ManyToOne    private  Driver driver; //here we have to define to whom this booking    @ManyToOne    private  Passenger passenger; //here we have to define to whom this booking}